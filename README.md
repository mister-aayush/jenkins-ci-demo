# jenkins-ci-demo

A demo app for Jenkins CI/CD pipeline practice.

## Files
- `app.sh` — prints system info and app version
- `test.sh` — runs app.sh and validates exit code
- `Jenkinsfile` — defines the CI/CD pipeline

## How to run locally
```bash
./app.sh
./test.sh
```
